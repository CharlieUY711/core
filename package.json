{
  "name": "core-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build --filter=core-landing"
  },
  "devDependencies": {
    "turbo": "1.13.3"
  },
  "engines": {
    "node": ">=20"
  }
}